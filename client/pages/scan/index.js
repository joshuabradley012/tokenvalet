import React from 'react';
import './style.scss';
import { Section } from 'components';

const Scan = () => (
  <Section className="scan-section">
    <h1>Scan</h1>
  </Section>
);

export default Scan;

